{
  "name": "pipeline-trabalho-aula6",
  "version": "1.0.0",
  "description": "Projeto JavaScript com pipeline de CI usando GitHub Actions.",
  "main": "src/ServicoDePagamento.js",
  "scripts": {
    "test": "mocha",
    "test:report": "mkdir -p reports && mocha --reporter mocha-junit-reporter --reporter-options mochaFile=reports/junit.xml && mocha --reporter mochawesome --reporter-options reportDir=reports,reportFilename=test-report,html=true,json=false"
  },
  "devDependencies": {
    "mocha": "^10.8.2",
    "mocha-junit-reporter": "^2.2.1",
    "mochawesome": "^7.1.3"
  }
}
